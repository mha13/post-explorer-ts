// Phase 3 — the real app. Built incrementally in the next steps.
function PostExplorerPage() {
  return (
    <article className="page">
      <h1>Post Explorer</h1>
      <p className="muted">
        Coming next: a typed React app that fetches posts, comments, and authors
        from the JSONPlaceholder API. We build it step by step — types first,
        then the typed API layer, then components.
      </p>
    </article>
  );
}

export default PostExplorerPage;
